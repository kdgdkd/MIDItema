# MIDI processing
mido>=1.2.10

# OSC communication
python-osc>=1.8.0

# Terminal UI framework
textual>=0.44.0

# JSON5 parsing (JSON with comments)
json5>=0.9.0

# JSON schema validation
jsonschema>=4.17.0

# Additional dependencies for cross-platform compatibility
# Windows MIDI support (included with mido)
# Unix terminal control (standard library: termios, tty, select)
# Windows console input (standard library: msvcrt)